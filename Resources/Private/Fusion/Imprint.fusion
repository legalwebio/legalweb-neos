prototype(LegalWeb.GdprTools:Imprint) < prototype(Neos.Neos:ContentComponent) {
    renderer = afx`
        <div>{LegalWeb.GdprTools.Services.getImprint()}</div>
    `

    @cache {
        mode = 'uncached'
        context {
            1 = 'node'
            2 = 'documentNode'
        }
    }
}
