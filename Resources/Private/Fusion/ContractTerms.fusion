prototype(LegalWeb.GdprTools:ContractTerms) < prototype(Neos.Neos:ContentComponent) {
    renderer = afx`
        <div>{LegalWeb.GdprTools.Services.getContractTerms()}</div>
    `

    @cache {
        mode = 'uncached'
        context {
            1 = 'node'
            2 = 'documentNode'
        }
    }
}
